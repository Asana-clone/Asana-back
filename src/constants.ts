export enum Authority {
  owner = 'owner',
  canEdit = 'canEdit',
  canComment = 'canComment',
}

export enum Status {
  complete = 'compleate',
  uncomplete = 'uncomplete',
  approved = 'approved',
  changesRequested = 'changesRequested',
  rejected = 'rejected',
}
